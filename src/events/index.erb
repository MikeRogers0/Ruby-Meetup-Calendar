---
layout: default
permalink: /events/:term/
template_engine: erb
prototype:
  collection: events
  term: year_month
---

<%
  require 'active_support/core_ext'
%>

<% page.data[:title] = "#{t('date.month_names')[Time.parse("#{page.data[:year_month]}-01").month]} #{Time.parse("#{page.data[:year_month]}-01").year}" %>
<%= partial 'calendar', locals: { month: Time.parse("#{page.data[:year_month]}-01") } %>
