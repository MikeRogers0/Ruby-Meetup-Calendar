---
layout: default
---

<div class="antialiased border-gray-100 container mx-auto py-2 md:py-12">
  <h1 class="text-center mb-5">{{ page.title }}</h1>

  <p>{{ page.name }}</p>
  <p>{{ page.datetime }}</p>
  <p>{{ page.external_url }}</p>

  {{ content }}
</div>
